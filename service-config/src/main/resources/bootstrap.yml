server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          url: https://github.com/BrayanBaquero/store-microservices-springCloud.git
          searchPath: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

